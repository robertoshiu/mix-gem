## 2026-05-06
- SmartFactory tokens should stay split into full-name `--smartfactory-*` definitions and `--sf-*` aliases.
- Keep `--gauge-value-color` only in the shared overlay block; duplicate top-level aliases cause drift.
- The muted text token needs the brighter `#64748B` value for contrast on the dark canvas.
